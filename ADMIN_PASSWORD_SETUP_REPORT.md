# 后台密码配置报告

## 当前状态

- 本地后端代码已改为从环境变量 `ADMIN_PASSWORD` 读取后台密码。
- 本地代码中没有写入真实后台密码。
- 服务器 `raz-backend.service` 已通过环境变量 `ADMIN_PASSWORD` 配置后台密码。
- 后端服务已重启成功。
- 线上 `/admin` 未登录访问会跳转到登录页。
- 线上 `/admin/export` 未登录访问会跳转到登录页。
- 错误密码无法登录。
- 正确密码可以登录后台。
- 登录后刷新 `/admin` 仍能看到后台。
- 登录后 `/admin/export` 可以导出 CSV。

## 安全检查

- 未在前端代码中写入后台密码。
- 未在本报告中写出真实密码。
- 未发现 `.env`、`submissions.db`、token 或 SSH 私钥被加入当前 Git 改动。

## 验收结论

- 后台密码已改为服务器环境变量配置。
- 后台登录保护正常。
- 导出接口受登录保护。
